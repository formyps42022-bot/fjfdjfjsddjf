name: Обновление данных LM.inc

on:
  schedule:
    # Каждые 15 минут. При желании можно изменить (минимум для GitHub Actions — 5 минут,
    # но частые запуски могут выполняться с задержкой на бесплатном тарифе).
    - cron: "*/15 * * * *"
  workflow_dispatch: {}
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  update-data:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Обновить data.json
        run: node scripts/update-data.mjs

      - name: Закоммитить изменения (если есть)
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          if git diff --quiet -- data.json; then
            echo "Нет изменений в data.json"
          else
            git add data.json
            git commit -m "chore: автообновление data.json"
            git push
          fi
